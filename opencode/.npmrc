# `npm version` tags as `v<version>` by default, which is the same namespace
# the repository's own Release workflow tags the Claude Code plugin in. Two
# release tracks, one tag namespace: the first npm bump to a version the
# plugin already released would collide. Prefix ours instead.
tag-version-prefix=cosa-opencode-v
